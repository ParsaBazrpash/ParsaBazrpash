# Upstash Redis (free tier): https://console.upstash.com/
# On Vercel: Storage → Create Database → Redis → connect to this project.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Optional: add past traffic if you already had visitors before enabling the counter.
VISITOR_COUNT_OFFSET=0
